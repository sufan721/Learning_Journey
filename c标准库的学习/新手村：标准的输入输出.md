	#include<stdio.h> 

大部分的函数和变量都是看**其名知其意**。这也是我喜欢c/c++的原因，**不必死记硬背，只需大概知道他有什么作用**，再用那初中英语给拼凑出来就好了
### **库变量：**
	size_t：无符号整型返回，对象空间大小(sizeof)
	FILE：文件流，存储文件流的对象类型
	fpos_t：文件位置
### **库宏：**
	NULL：空
	EOF：文件结束的负整数
	FOPEN_MAX：同时可以打开的文件数量**fopen_max**
	FILENAME_MAX：最大的文件名长度**flie_max**
	stderr、stdin、stdout：指向FILEL类型的指针对应错误、输入、输出
### **库函数：**
###### int fclose(FILE *a){};
	关闭文件，成功返回0，否则返回EOF
	#include <stdio.h> 
	int main(){
		FILE *fp;
		fp=fopen("text.txt",w);
		fprintf(fp,"%s","开始我们的学习之旅");
		fclose(fp);
		return 0;
	}
###### void clearerr(FILE *a){};
	清除给定流 a 的文件结束和错误标识符。
	#include <stdio.h>
	int main()	{
	   FILE *fp;
	   char c;
	   fp = fopen("file.txt", "w");
	   c = fgetc(fp);
	   if( ferror(fp) ) {
	      printf("读取文件：file.txt 时发生错误\n");
	   }
	   clearerr(fp);
	   if( ferror(fp) ){
	      printf("读取文件：file.txt 时发生错误\n");
	   }
	   fclose(fp);

	   return(0);
	}
###### int feof(FILE *a){}
	测试文件流a的文件结束标识符/是否阶数
	返回：当设置了与流关联的文件结束标识符时，该函数返回一个非零值，否则返回零。
###### int ferror(FILE *a){};
	测试文件流a的错误标识符。
	返回：如果设置了与流关联的错误标识符，该函数返回一个非零值，否则返回一个零值。
###### int fflush(FILE *a){};
	刷新输出缓冲区。
	
###### int fget(FILE *a,fpos_t *pos){};

###### FILE *fopen(const char *name ,const char * mode){};

###### size_t fread(void * ptr, size_t size, size_t nem ,FILE *a){};

###### FILE *freopen(const  char *filename,const char *mod,FILE *a){};

###### int fseek(FILE *a,long int off,int whence){};

###### int fsetpos(FILE *a,const fpos_t *pos){};

###### long int ftell(FILE *a){};

###### size_t fwrite(const void *ptr,size_t size(), size()_t nem,FILE *a){};

###### int remove(const char *filename){};

###### int rename(const char *pre_filename , const char *new_filename){};

###### void rewind(FILE *a){};

###### void setbuff(FILE *a ,char *buffer){};

###### int setvbuff(FILE *a, char & buff ,int mode,size_t size()){} ;

###### int setvbuf(FILE *stream, char *buffer, int mode, size_t size){};

###### FILE *tmpfile(void){};

###### char *tmpnam(char *str){};

###### int fprintf(FILE *stream, const char *format, ...){};

###### int printf(const char *format, ...){};

###### int sprintf(char *str, const char *format, ...){};

###### int vfprintf(FILE *stream, const char *format, va_list arg){};

###### int vprintf(const char *format, va_list arg){};

###### int vsprintf(char *str, const char *format, va_list arg){};

###### int fscanf(FILE *stream, const char *format, ...){};

###### int scanf(const char *format, ...){};

###### int sscanf(const char *str, const char *format, ...){};

###### int fgetc(FILE *stream){};

###### char *fgets(char *str, int n, FILE *stream){};

###### int fputc(int char, FILE *stream){};
###### int fputs(const char *str, FILE *stream){};

###### int getc(FILE *stream){};

###### int getchar(void){};

###### char *gets(char *str){};

###### int putc(int char, FILE *stream){};

###### int putchar(int char){};

###### int puts(const char *str){};

###### int ungetc(int char, FILE *stream){};

###### void perror(const char *str){};

###### int snprintf(char *str, size_t size, const char *format, ...){};

### 总结

碎碎念：写这些这快给我写死了，我真的不知道会有怎么多函数，早知道不写了，俺不得劲！！！
/ᐠ - ˕ -マ