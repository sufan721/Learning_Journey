### 变量：
- 和**c语言**以及其他语言一样的命名规则
- 等号两边***不能取空格***
- 变量名大写，规范，小写也没问题

### 命令的返回值赋给变量：
- A='data'反引号，运行里面的命令，并把结果返回给变量A
- A=$(data)等价于反引号

### 位置参数变量：
**得到命令行的参数信息**
```
$n：获取输入的第n个参数，大于9要加{}
$*：获取命令行的所有参数，看成一个整体(字符串)
$@：获取命令行的所有参数，区分对待
$#：获取参数的个数
```