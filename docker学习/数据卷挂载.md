因为docker的应用只有最小化的运行环境(甚至连落后的vim都没有)，因此需要一个桥梁去连接容器目录和主机目录-> 那就是数据卷(虚拟目录)

![ 数据卷命令](文档图片/数据卷.png )

#### 如何挂载
在执行docker run 命令的时候使用-v 数据卷:容器内目录如果数据卷不存在会自动创建
-v可以使用路径(相对和绝对)
只有在docker run命令执行时才能进行挂载
例子：docker run -d --name nginx -p 80:80 -v html:/user/share/nginx/html nginx