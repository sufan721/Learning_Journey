### **CEIL(ceil)**(x)
```markdown
向上取整
```
### **FLOOR(floor)**(x)
```markdown
向下取整
```
### **MOD(mod)**(x,y)
```markdown
返回x/y的模
```
### **RAND(rand)**()
```markdown
返回0~1内的随机数
```
### **ROUND(round)**(x,y)
```markdown
求参数x的四舍五入的值，保留y位小数
```