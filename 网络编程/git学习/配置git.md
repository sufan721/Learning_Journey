既然你决定使用git，那么请先让git知道你是谁

---
## 1.配置身份
- **设置用户名**
```bash
git config --global user.name "你的名字"
```
- **设置登录邮箱 **
```bash
  git config --global user.email "your_email@example.com"
```
## 2.查看是否配置正确
- **查看配置信息**
```bash
git config --list
```